
+ structure
  # dans les fonctions set_* (personne, acte, relation etc.)
    renvoyer l'objet php et non l'id mysql

  + dans l'objet Acte
    # champ $personnes contenant la liste des personnes mentionnées dans l'acte
    # dans into_db, une fois que l'acte est enregistré dans la base,
      donner le signal aux personne d'enregistrer leurs conditions et
      leurs relations

  + seulement dans l'objet Personne,
    # champs $conditions et $relations
      contenant la liste des conditions / relations de cette personne définies dans l'acte courant
    # methodes enregister_relations / conditions qui appellent les
      into_db des relations et conditions
    # si pas d'id, chercher d'abord une personne avec les mêmes
      prénoms et noms avant de créer une nouvelle personne

+ création
  - id max à partir des csv (dans Dropbox/SiteWeb/data) pour personnes
  - parrains, sur le modèle de temoins

+ optimisation
  # SELECT LAST_INSERT_ID() (au lieu de ORDER BY dans get_last_id), et
  dans la même fonction que le INSERT (et dans une seule fonction ou
  alors appelé une seule fois)
  # vérifier pourquoi 2 fois requête personne au moment de l'ajout

+ consultation
  - personne
  - acte

+ tests
  - 10 actes Belgrano
  - acte sans les ids
  - acte sans balises nom / prenom, puis le même acte, avec un nom / prenom
  - 100 actes

+ gestion d'erreurs
  # fichiers



+ Ced
  - condition décès
  - veuf/veuve
  - bouton loading add actes
